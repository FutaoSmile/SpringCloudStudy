spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    open-in-view: true
  datasource:
    url: jdbc:mysql://114.215.173.71/spring_cloud_learn?useUnicode=true&characterEncoding=utf8
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: HelloMysql123.
  cloud:
    consul:
      enabled: true
      host: 114.215.173.71
      port: 8500
      discovery:
        instance-id: ${spring.application.name}-${random.uuid}
  boot:
    admin:
      client:
        url: http://114.215.173.71:8080

server:
  port: 7010

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8881/eureka/

debug: false

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: com.futao.biz.user.entity
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: true
    db-config:
      id-type: uuid

app:
  id: ${spring.application.name}    # 与apollo管理后台配置的项目的appId一致
  apollo:
    username: 1
    age: 1
apollo:
  meta: http://localhost:8080
#  bootstrap:
#    enabled: true
#    namespaces: application,TEST1.base   # 该app(appId)下的某个命名空间(某个配置文件)

apl:
  conf1: 1
  conf2: 1
  conf3: 1
  conf4: 1