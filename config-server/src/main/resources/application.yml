server:
  port: 8887
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FutaoSmile/SpringCloudConfigRepostory.git # 配置git仓库地址
          search-paths: respo       # 配置仓库路径
      label: master           # 配置仓库的分支